pipeline {
    agent any
    tools {
        maven 'Maven_3_9_11'
    }
    stages {
        stage('Build with Maven') {
            steps {
                sh 'mvn clean install -DskipTests'
		echo 'Building'
            }
        }
	stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
    }
    // ... other stages (Test, Deploy)
